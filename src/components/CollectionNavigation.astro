---
import {
	getCollectionNavigation,
	formatCollectionTitle,
} from "../utils/collectionNavigation";
import { transformId } from "../utils/transformId";

interface Props {
	collection: string;
}

const { collection } = Astro.props;
const navigation = getCollectionNavigation(collection);
---

{
	(navigation.prev || navigation.next) && (
		<nav
			class="relative flex flex-col sm:flex-row gap-4 sm:gap-8 w-full my-6"
			data-collection-footer-nav
		>
			<div class="w-full sm:w-1/2 flex justify-start">
				{navigation.prev && (
					<a
						href={`/${navigation.prev.slug}`}
						class="nav-link w-fit p-3 rounded-lg border border-[color:var(--surface-border)] bg-[var(--surface-elevated)] text-sm shadow-md hover:shadow-lg transition-shadow duration-200 text-left"
						style="color: var(--link-color)"
						data-base-href={`/${navigation.prev.slug}`}
						data-collection-nav="prev"
						title="Previous collection"
						aria-keyshortcuts="["
					>
						← {transformId(navigation.prev.slug)} -{" "}
						{formatCollectionTitle(navigation.prev.title)}
					</a>
				)}
			</div>
			<div class="w-full sm:w-1/2 flex justify-end">
				{navigation.next && (
					<a
						href={`/${navigation.next.slug}`}
						class="nav-link w-fit p-3 rounded-lg border border-[color:var(--surface-border)] bg-[var(--surface-elevated)] text-sm shadow-md hover:shadow-lg transition-shadow duration-200 text-right"
						style="color: var(--link-color)"
						data-base-href={`/${navigation.next.slug}`}
						data-collection-nav="next"
						title="Next collection"
						aria-keyshortcuts="]"
					>
						{transformId(navigation.next.slug)} -{" "}
						{formatCollectionTitle(navigation.next.title)} →
					</a>
				)}
			</div>
		</nav>
	)
}

<style>
	@media (hover: hover) and (pointer: fine) {
		.nav-link[data-collection-nav="prev"]:hover::after,
		.nav-link[data-collection-nav="prev"]:focus-visible::after,
		.nav-link[data-collection-nav="next"]:hover::after,
		.nav-link[data-collection-nav="next"]:focus-visible::after {
			display: inline-block;
			margin-left: 0.45em;
			padding: 1px 5px;
			border: 1px solid var(--border-color);
			border-bottom-width: 2px;
			border-radius: 4px;
			background: var(--background-color);
			color: var(--text-muted);
			font:
				600 10px/1.4 system-ui,
				-apple-system,
				Segoe UI,
				Roboto,
				sans-serif;
			vertical-align: 0.1em;
			opacity: 0.8;
		}

		.nav-link[data-collection-nav="prev"]:hover::after,
		.nav-link[data-collection-nav="prev"]:focus-visible::after {
			content: "[";
		}

		.nav-link[data-collection-nav="next"]:hover::after,
		.nav-link[data-collection-nav="next"]:focus-visible::after {
			content: "]";
		}
	}

	@media print {
		.nav-link[data-collection-nav]::after {
			content: none;
		}
	}
</style>

<script>
	function isEditableTarget(el: Element | null): boolean {
		if (!el || !(el instanceof HTMLElement)) return false;
		const tag = el.tagName.toLowerCase();
		if (el.isContentEditable) return true;
		if (tag === "textarea" || tag === "select") return true;
		if (tag === "input") {
			const type = (el as HTMLInputElement).type?.toLowerCase() ?? "text";
			if (type === "range") return false;
			return true;
		}
		return false;
	}

	if (!(window as unknown as { __collectionNavBracketKeys?: boolean })
		.__collectionNavBracketKeys) {
		(window as unknown as { __collectionNavBracketKeys: boolean }).__collectionNavBracketKeys =
			true;
		document.addEventListener("keydown", (e: KeyboardEvent) => {
			if (e.ctrlKey || e.metaKey || e.altKey) return;
			if (isEditableTarget(document.activeElement)) return;

			const root = document.querySelector("[data-collection-footer-nav]");
			if (!root) return;

			if (e.key === "[") {
				const link = root.querySelector(
					'a[data-collection-nav="prev"]',
				) as HTMLAnchorElement | null;
				if (!link || link.classList.contains("hidden")) return;
				e.preventDefault();
				window.location.assign(link.href);
				return;
			}
			if (e.key === "]") {
				const link = root.querySelector(
					'a[data-collection-nav="next"]',
				) as HTMLAnchorElement | null;
				if (!link || link.classList.contains("hidden")) return;
				e.preventDefault();
				window.location.assign(link.href);
			}
		});
	}
</script>
