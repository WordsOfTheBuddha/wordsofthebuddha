---
const { description } = Astro.props;
---

{
	description && (
		<div class="my-2 mt-4 p-4 rounded-lg border bg-[var(--callout-background)] border-[var(--border-color)] w-[fit-content]">
			<p class="my-0 text-lg">{description}</p>
		</div>
	)
}
