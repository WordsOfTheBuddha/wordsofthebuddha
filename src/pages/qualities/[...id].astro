---
export const prerender = false;
import qualityMappings from "../../data/qualityMappings.json";

const { id } = Astro.params;
if (!id || !(id in qualityMappings)) {
	return Astro.redirect("/qualities");
}

return Astro.redirect(`/on/${id}`, 301);
---
