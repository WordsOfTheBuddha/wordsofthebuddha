---
export const prerender = false;
/**
 * Legacy / alternate entry for the signed-in home.
 * Overview is the default Review Room tab (no hash needed).
 */
return Astro.redirect("/review-room");
---
